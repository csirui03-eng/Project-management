# Servers

Started 2026-08-22. Concept note beside the numbered steps, feeds [[3 Solver at start|Solver at start]].

## What a server is

A server is a process that on start claims an address and does no work until a request arrives at that address from a client. It answers, then stays running, so the next request finds it there. Of course, being a process, when it is started it lives in memory.

## Server and client in MAPDL terms

## IP, port, PID

Reference if wanted: [[2026-08-22 Servers, LLM draft]].
