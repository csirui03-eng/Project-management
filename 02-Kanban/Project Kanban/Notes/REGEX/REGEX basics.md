## Regular expressions (REGEX)
A system first implemented by Ken Thomspon in the Unix text editor as ed, then implemented into grep ("Global regular expression print") which is still used today in mac and linux systems

