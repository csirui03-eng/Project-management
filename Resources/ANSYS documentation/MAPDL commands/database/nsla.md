---
apdl: "NSLA"
method: nsla
group: database
source: PyMAPDL 0.74.1 docstrings, pandoc 3.9.0.2, ansys.mapdl.core._commands.database.selecting.Selecting.nsla
generated: 2026-08-22
tags: [mapdl-command]
---

# NSLA

PyMAPDL: `mapdl.nsla(type_='', nkey='', **kwargs)`

Selects those nodes associated with the selected areas.

## Parameters

**type_**

Label identifying the type of node select:

- `S` - Select a new set (default).
- `R` - Reselect a set from the current set.
- `A` - Additionally select a set and extend the current set.
- `U` - Unselect a set from the current set.

**nkey**

Specifies whether only interior area nodes are to be selected:

- `0` - Select only nodes interior to selected areas.
- `1` - Select all nodes (interior to area, interior to lines, and at keypoints) associated with the selected areas.

## Notes

Valid only if the nodes were generated by an area meshing operation ( [[amesh|AMESH]], [[vmesh|VMESH]] ) on a solid model that contains the selected areas.

This command is valid in any processor.

Ansys help: https://ansyshelp.ansys.com/Views/Secured/corp/v232/en//ans_cmd/Hlp_C_NSLA.html
