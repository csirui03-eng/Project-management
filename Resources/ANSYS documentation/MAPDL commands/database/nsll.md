---
apdl: "NSLL"
method: nsll
group: database
source: PyMAPDL 0.74.1 docstrings, pandoc 3.9.0.2, ansys.mapdl.core._commands.database.selecting.Selecting.nsll
generated: 2026-08-22
tags: [mapdl-command]
---

# NSLL

PyMAPDL: `mapdl.nsll(type_='', nkey='', **kwargs)`

Selects those nodes associated with the selected lines.

## Parameters

**type_**

Label identifying the type of node select:

- `S` - Select a new set (default).
- `R` - Reselect a set from the current set.
- `A` - Additionally select a set and extend the current set.
- `U` - Unselect a set from the current set.

**nkey**

Specifies whether only interior line nodes are to be selected:

- `0` - Select only nodes interior to selected lines.
- `1` - Select all nodes (interior to line and at keypoints) associated with the selected lines.

## Notes

Valid only if the nodes were generated by a line meshing operation ( [[lmesh|LMESH]], [[amesh|AMESH]], [[vmesh|VMESH]] ) on a solid model that contains the associated lines.

This command is valid in any processor.

Ansys help: https://ansyshelp.ansys.com/Views/Secured/corp/v232/en//ans_cmd/Hlp_C_NSLL.html
